@model SelectInstancesViewModel

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0 rounded-3">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Metabase Migrator</h4>
                </div>
                <div class="card-body">
                    <form method="post" asp-action="SelectInstances" asp-controller="Dashboard">
                        <div class="mb-3">
                            <label for="sourceSelect" class="form-label">Source Instance</label>
                            <select asp-for="SourceUrl" id="sourceSelect" name="sourceApiUrl" class="form-select">
                                @foreach (var src in Model.Sources)
                                {
                                    <option value="@src.Url">@src.Name</option>
                                }
                            </select>
                        </div>

                        <div class="mb-3">
                            <label for="targetSelect" class="form-label">Target Instance</label>
                            <select asp-for="TargetUrl" id="targetSelect" name="targetApiUrl" class="form-select">
                                @foreach (var tgt in Model.Targets)
                                {
                                    <option value="@tgt.Url">@tgt.Name</option>
                                }
                            </select>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-success">
                                <i class="bi bi-arrow-right-circle"></i> Proceed
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
